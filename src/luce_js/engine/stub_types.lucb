#==============================================================================================
#
#   stub_types - Not yet ported: placeholders for types declared inside regions
#
#   DESCRIPTION:
#       Each placeholder is deleted when the fragment that ports its region declares the
#       real type. The file is deleted when it is empty.
#
#==============================================================================================

struct AsyncGeneratorData:
    var unported: i32

struct GeneratorData:
    var unported: i32

struct JSONParseRecord:
    var unported: i32

struct JSONParseRecordObject:
    var unported: i32

struct JSONStringifyContext:
    var unported: i32

struct RegExpStringIteratorData:
    var unported: i32

struct ValueBuffer:
    var unported: i32

# mark: Declared by other regions ===============================================================

# quickjs.c:50574 (region r25): the Reflect object's table, which the base intrinsics
# (r28) install; region r25 deletes this placeholder.
let js_reflect_obj: CFunctionListEntry[1] = [CFunctionListEntry(name = "unported", def_type = def_prop_undefined)]
